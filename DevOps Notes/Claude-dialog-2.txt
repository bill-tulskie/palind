ownerId, field_city_of_birth, field_clinical, field_date_of_birth, field_account_first_name, field_gender_at_birth, field_diagnosis_gene, field_account_last_name, field_account_middle_name, uid, mail

Next, we need to update preprocess_data.py to generate data sets for upload from raw csv files that will include the source data for the new clinical_classification column in the upload dataset.  The columns in the raw csv file include: ownerId, field_city_of_birth, field_clinical, field_date_of_birth, field_account_first_name, field_gender_at_birth, field_diagnosis_gene, field_account_last_name, field_account_middle_name, uid, mail.  The output dataset should include the columns in the upload dataset, specifically: id, first_name, last_name, date_of_birth, middle_name, sex_at_birth, city_at_birth, gene, clinical_classification. The input fields should be mapped to the output fields as follows: ownerId should be ignored, field_city_of_birth = city_at_birth, field_clinical = clinical_classification, field_date_of_birth = date_of_birth, field_account_first_name = first_name, field_gender_at_birth = sex_at_birth, field_diagnosis_gene = disease_id, field_account_last_name = last_name, field_account_middle_name = middle_name, uid = id, mail can be ignored. The order of the columns in the output dataset should match the order of the columns in the upload data specification.


The upload data model now includes the following fields: id, first_name, last_name, date_of_birth, middle_name, sex_at_birth, city_at_birth, gene, clinical_classification. The upload data model should be: id, disease_id, first_name, last_name, date_of_birth, middle_name, sex_at_birth, city_at_birth, clinical_classification where gene in the current data model corresponds to disease_id in the new model.

disease_id in the upload model should not be tokenized by the tokenizer.  It should be uploaded as is so that the prevalence satistics can be correctly calculated.  Also, the clinical_classification data should not be tokenized so that prevalence statistics will be generated for that field similarly to disease_id.

Statistics for the clinical_classification instances in the upload data should be generated for that field similarly to disease_id and should be added to the prevalence dashboard

Add a help function to the preprocess_data.py program which provides an explanation of how to invoke the program, lists the expected fields in the input dataset, and explains the output files. This help function should be invoked by running the program as a command with a -h flag or a --help flag.

Next, we need to revise the fields that will appear in the input dataset for preprocess_data.py.  The input fields in the new expected order are: ownerId, field_city_of_birth, field_clinical,	field_date_of_birth, field_account_first_name, field_gender_at_birth,	field_diagnosis_gene,	field_account_last_name,	field_account_middle_name,	uid,	mail.  The uid field in the input file should be mapped to the id field in the output file. The field field_clinical should be mapped to a new field in the output file called clinical_classification. The ownerId and mail fields in the input file should be ignored.

When processing the field field_clinical in the input file, the program should search the label column in the prevalence_clinicaldx table for a match to the value in the field field_clinical, should retrieve the corresponding value in the clinical_classification column in the prevalence_clinicaldx table and should write the retrieved value to the clinical_classification column in the output file.

If the label look up does not find a match, set the value of the clinical_classification to not_found.  If the field_clinical in the input data set is blank or null, do not attempt a look up. Just leave the clinical_classification blank null in the output file too.

When preprocess_data.py is reading the values in the field field_city_of_birth, the input values sometimes contain the city name and the country or state separated by a comma.  The comma and the state or country should not be written to the output file.  One exception is when the city is Washington, DC.  In that case, remove the comma but keep 'DC' in value written to the output dataset. In other cases, the state or country is included in parentheses after the city name.  In this case the country name and parentheses should not be written to the output file should be written. In these cases where the city name requires this extra processing, include a record and explanation in the no_profile output file.

These is an additional case that should be handled by preprocess_data.py.  When the field field_city_of_birth contains a single character, usually a '?' or a '.', leave the value null in the output file but note those occurrences with an explanation in the no_profile output file.

Next, we need to build a new python program that can be invoked similarly to the import and preprocess programs in the commands folder.  This program should be called generate_duplicate_records and should take as input a file that is of the same format as the output from preprocess_data.py.  For each record in the input file, the program should emit the identical record and then generate 3 additional records. In the first additional record, replace the first name with a common nickname for that first name. Also, append '-first' to the id value. In the second additional record, replace the middle name with a common nickname for that name.  If there is no middle name, add a middle name that is not the same as the first name or last name.  Also append '-middle' to the value for the id for this record. For the third additional record, replace the middle name with the first letter of the middle name.  If there is no middle name in the input record, change to a single letter or a blank chosen at random.  Also, append '-middle2' to the value for the id for this third added record.

Please add user help to the generate_duplicate_records command that is invoked with the -h or --help flag and that provides the command format.  The user help should also explain the different additional records that are generated.

At present, the two links under the DEMO heading in the sidebar are only available to users with superuser permissions. Can you confirm that the Tokenizer demo page does not affect the tokenization processing of actual uploaded data.  Specifically can you confirm that the q-gram setting, the bloom filter length, and the epsilon value are only used in the demonstration on the page?

In the page accessed at URL datasets, there is a left hand sidebar that includes a link to the Incidence Dashboard.  There is a title over this link that says 'Prevalence'. it should say 'Incidence' instead.

On the admin interface sidebar under the DEMO heading, there is a link to Tokenizer that accesses this url: bloom-filter-demo. Clicking the link causes an error page to be shown.  The error page title is TemplateDoesNotExist at /bloom-filter-demo.  the exception location is: /usr/local/lib/python3.11/site-packages/django/template/backends/django.py, line 84, in reraise.  the exception is raised in django.views.generic.base.TemplateView in python excecutable /usr/local/bin/python.  Can you correct this? Also make this more robust by removing the external CDN dependency and embedding a local SHA-256 implementation in the project static files.

The two links and the DEMO heading only appear for users with Superuser status.  Please show the heading and the links in the  sidebar for all users including users with staff status and users without Staff status or Superuser status.

Next, we need allow users to optionally upload data in the format accepted by the preprocess_data.py command. We will do this in a few incremental steps.  First, we need a version of the preprocess_data.py command that is functionally equivalent but written in javascript and that produces its output in an in-memory array which is displayed in a table in a browser window. Can you complete this first step?
